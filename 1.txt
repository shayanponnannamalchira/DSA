Algorithm for Binary Search
1. Set low=0, high=n-1
2. While low<=high:
         a. mid=(low+high)/2
         b. if A[mid]==x->return mid
         c. if A[mid]<x->low=mid+1
         d. else->high=mid-1
3. if not found, return-1